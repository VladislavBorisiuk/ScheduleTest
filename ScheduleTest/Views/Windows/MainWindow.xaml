<Window x:Class="ScheduleTest.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" mc:Ignorable="d"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:ScheduleTest.ViewModels"
        xmlns:mod ="clr-namespace:ScheduleTest.Models"
        xmlns:con ="clr-namespace:ScheduleTest.Infrastructure.Converters"
        xmlns:l="clr-namespace:ScheduleTest"
        d:DataContext="{d:DesignInstance Type=vm:MainWindowViewModel}"
        DataContext="{Binding MainWindowModel, Source={StaticResource Locator}}"
        
        Title="{Binding Title}"
        Width="800" Height="450">
    <DockPanel>
        <Menu DockPanel.Dock="Top" IsMainMenu="True">
            <Menu.ItemsPanel>
                <ItemsPanelTemplate>
                    <DockPanel LastChildFill="False"/>
                </ItemsPanelTemplate>
            </Menu.ItemsPanel>
            <MenuItem Header="Файл">
                <MenuItem Header="Выход" Command="{StaticResource CloseWindowCommand}"/>
            </MenuItem>
        </Menu>
        <StatusBar DockPanel.Dock="Bottom">
            <StatusBarItem DockPanel.Dock="Right">
                <ResizeGrip/>
            </StatusBarItem>
            <StatusBarItem Content="{Binding Status}"/>
        </StatusBar>
        <Grid>
            <ItemsControl Name="TasksContainer" ItemsSource="{Binding Models}">
                <ItemsControl.Resources>
                    <con:StartTimeToXPositionConverter x:Key="StartTimeToXPositionConverter"/>
                    <con:TaskTypeToColorConverter x:Key="TaskTypeToColorConverter"/>
                </ItemsControl.Resources>
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <!-- Используйте StackPanel или WrapPanel -->
                        <StackPanel Orientation="Horizontal"/>
                        <!-- Пример использования WrapPanel:
            <WrapPanel Orientation="Horizontal" ItemWidth="100" ItemHeight="30"/> -->
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate DataType="{x:Type mod:TaskModel}">
                        <TextBlock Text="{Binding Name}" Width="{Binding Path=Wight}" MinWidth="50" MaxWidth="300" Height="30" 
                       Background="{Binding Type, Converter={StaticResource TaskTypeToColorConverter}}">
                            <TextBlock.RenderTransform>
                                <TranslateTransform>
                                    <TranslateTransform.X>
                                        <MultiBinding Converter="{StaticResource StartTimeToXPositionConverter}">
                                            <Binding Path="StartTime"/>
                                            <Binding Path="DeadLine"/>
                                        </MultiBinding>
                                    </TranslateTransform.X>
                                </TranslateTransform>
                            </TextBlock.RenderTransform>
                        </TextBlock>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </Grid>
    </DockPanel>
</Window>
